import { FullScreenLoader } from "@/components/full-screen-loader";

const LoadingPage = () => {
  return (
    <div>
      <FullScreenLoader label="Be patience! Document is loading ..." />
    </div>
  );
};

export default LoadingPage;
